export interface LoanData {
    loanAmount: number;
    interestRate: number; // Als Prozentwert (z.B. 2.12)
    initialRepayment: number; // Als Prozentwert (z.B. 2)
    interestFixation: number; // In Jahren
}
